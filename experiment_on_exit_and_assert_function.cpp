#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main()
{

    cout<<"This is one"<<endl;
    //exit(0);//this function terminate the process normally
    //if condition is true then if will continue the whole program
    assert(321 == 32); //use of assert function
    cout<<"This is checking for assert program"<<endl;
 return 0;
}
